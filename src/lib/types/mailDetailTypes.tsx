export interface MailDetailData {
  title: string;
  letterImageUrl: string;
  createdAt: string;
  sender: string;
}

export interface MailText {
  originalText: string[];
  translatedText: string[];
}
